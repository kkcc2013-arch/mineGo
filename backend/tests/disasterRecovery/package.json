{
  "name": "minego-disaster-recovery-tests",
  "version": "1.0.0",
  "description": "mineGo 灾备模块单元测试",
  "scripts": {
    "test": "mocha --timeout 15000 --reporter spec",
    "test:coverage": "nyc mocha --timeout 15000 --reporter spec",
    "test:dr": "node run-tests.js"
  },
  "dependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "sinon": "^15.0.1",
    "proxyquire": "^2.1.3"
  },
  "devDependencies": {
    "nyc": "^15.1.0"
  }
}